services:
  bot_student:
    build: .
    environment:
      - BOT_TYPE=student
      - TARGET_URL=https://10.10.100.4
    networks:
      gns3_bridge:
        ipv4_address: 10.10.250.4

  bot_professor:
    build: .
    environment:
      - BOT_TYPE=professor
      - TARGET_URL=https://10.10.100.4
    networks:
      gns3_bridge:
        ipv4_address: 10.10.250.5

  bot_ddos:
    build: .
    environment:
      - BOT_TYPE=ddos
      - TARGET_URL=https://10.10.100.4
    networks:
      gns3_bridge:
        ipv4_address: 10.10.250.6

networks:
  gns3_bridge:
    driver: macvlan
    driver_opts:
      parent: bond0
    ipam:
      config:
        - subnet: 10.10.250.0/29
          gateway: 10.10.250.1
              
